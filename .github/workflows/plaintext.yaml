name: Plaintext Secret Test
on: [push]

jobs:
  expose-secret:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
      - name: Export obvious secret
        env:
          API_KEY: "THIS_IS_A_DEMO_SECRET_DO_NOT_USE"
        run: |
          echo "Using API key $API_KEY"   # simulating secret in env (should be flagged by policy)
